from django import forms

# TODO: define the input your app accepts, e.g. a course picker, a
# "days ahead" number, a text filter. Example:
#
# class MyFilterForm(forms.Form):
#     days_ahead = forms.IntegerField(label="Days ahead", min_value=1, max_value=90, initial=7)
